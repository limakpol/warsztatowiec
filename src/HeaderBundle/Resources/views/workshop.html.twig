{{ form_start(form) }}
    {{ form_label(form.workshop.name) }}
    {{ form_widget(form.workshop.name, {'attr': {'autofocus': true}}) }}
    {{ form_widget(form._token) }}
    {{ form_widget(form.submit) }}
{{ form_end(form, {'render_rest': false}) }}